#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - prints single digit of base 10 starting 0
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);
}
